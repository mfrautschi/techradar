import { DraftHighlightDirective } from './draft-highlight.directive';

describe('DraftHighlightDirective', () => {
  it('should create an instance', () => {
    const directive = new DraftHighlightDirective();
    expect(directive).toBeTruthy();
  });
});
